# Contracts

This directory contains the external APIs and interface definitions. Since the Vodafone Cash payment module is purely internal to the web app frontend and the Appwrite database implementation, no external SDK schemas or APIs are produced here.
